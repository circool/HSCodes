
import ru.tsurkanenko.vladimir.hscodes.HarmBase;
import ru.tsurkanenko.vladimir.hscodes.HarmItem;

public class HarmItemTest {
    public static void main(String[] args) {
        final HarmBase demo = new HarmBase();
        /*
        for (HarmItem x:demo.getItems().getItemArray()
             ) {
            if(x.getDescription().startsWith("---"))
                System.out.println(x.getCode() + " " + x.getDescription());
        }


         */
    }
}
